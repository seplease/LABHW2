#include <stdio.h>
int main(void)
{
	int number;
	number = 7;
	printf("The square number of %d is %d\n", number, number * number);
	printf("The cube number of %d is %d", number, number * number * number);
}